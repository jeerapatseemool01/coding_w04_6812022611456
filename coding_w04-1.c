#include <stdio.h>          // เรียกใช้ไลบรารีมาตรฐาน

int main() {               // ฟังก์ชันหลักของโปรแกรม 
    printf("Hello,\n");     // แสดงข้อความ "Hello,"
    printf("C Tokens!\n");  // แสดงข้อความ "C Tokens!"
    return 0;               // ส่งค่ากลับว่าโปรแกรมทำงานจบเรียบร้อย
}