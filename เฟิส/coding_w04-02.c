#include <stdio.h>          // เรียกใช้ไลบรารีมาตรฐาน

int main() {                // ฟังก์ชันหลักที่ระบบจะเริ่มทำงาน
    int a = 10;             // ประกาศตัวแปรชนิด int ชื่อว่า a 
    printf("a = %d", a);    // แสดงผลค่าของตัวแปร a 
    return 0;               // ส่งค่ากลับ 0 
}
